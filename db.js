var config = {
  database: {
    host: '',//IP of the Database for external one / LocalHost for local
    user: '',//DB username
    password: '',//DB password
    port: '',// DB port
    db: ''// Name of DB
  }
};

module.exports = config;
